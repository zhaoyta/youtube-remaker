# 钓鱼教学课 BGM 素材

已从 [open-lofi](https://github.com/btahir/open-lofi)（**CC0**，可商用免署名）筛了 18 首轻底、无歌词曲，放在：

`skills/fishing-lesson-video/assets/bgm/`

- 默认推荐：`default.mp3` → `temple-at-dawn.mp3`（东方禅意，贴口播）
- `*.mp3` 已被 `.gitignore`，只在本机
- **成片已自动混入**：`build.py` 默认用 `default.mp3`，音量约 0.12；换曲 `--bgm soft-gold-sky.mp3`，关掉 `--no-bgm`

偏抖音口播下压的优先听：`temple-at-dawn` / `teacup-morning-fog` / `soft-gold-sky` / `sea-glass-evening` / `quiet-credits`。
